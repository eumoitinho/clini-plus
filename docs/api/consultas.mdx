# Consultas

**Base:** `/api/consultas`

## Endpoints
- `GET /api/consultas` — listar consultas.
- `GET /api/consultas/:id` — detalhes da consulta.
- `POST /api/consultas/:id/sala` — criar/obter sala de vídeo.
