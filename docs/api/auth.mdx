# Auth

**Base:** `/api/auth`

## Endpoints
- `GET /api/auth/[...nextauth]` — fluxo principal do NextAuth.

## Observações
- Configurar Azure AD B2C como provider principal.
- Páginas customizadas em `/login`, `/cadastro` e `/recuperar-senha`.
